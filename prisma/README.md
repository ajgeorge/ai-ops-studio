# Prisma

This folder contains the PostgreSQL schema and seed script for AI Ops Studio.

Useful commands:

```bash
npm run db:generate
npm run db:push
npm run db:seed
npm run db:studio
```

The seed script creates realistic demo data for Requirements Engine, Operations Copilot, RAG Evaluation Lab, AI runs, prompt versions, and audit logs.
