# Database Schema

The database slice will introduce Prisma models for:

- AI runs and prompt versions
- Audit logs
- Requirements projects and generated planning artifacts
- Operations jobs, technicians, service types, recommendations, and approvals
- RAG documents, chunks, answers, citations, test cases, and evaluation runs

The first slice keeps this document as a schema planning surface while the app shell is established.
