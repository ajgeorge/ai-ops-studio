# Deployment

Local development will use:

- Vite for the frontend
- Express for the API
- PostgreSQL and Redis through Docker Compose in a later slice
- Mock AI mode by default

Production deployment notes will be expanded after Docker, CI, and database migrations are in place.
